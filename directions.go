package main

import (
	"fmt"
)

func dirReduc(arr []string) []string {
	
}

func main() {
	lista := []string{"NORTH", "SOUTH", "SOUTH", "EAST", "WEST", "NORTH", "WEST"}
	fmt.Println(dirReduc(lista))
}